/**
 * @jest-environment jsdom
 */
import { render, screen } from "@testing-library/react";
import Page from "./page";

describe("메인페이지 테스트", () => {
  it("ㅋㅋ", () => {
    render(<Page />);
  });
});
